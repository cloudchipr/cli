export enum CloudProvider {
    AWS = 'aws',
}

export enum Region {
    US_EAST_1 = 'us-east-1',
}

export enum Profile {
    DEFAULT = 'default',
}

export enum Output {
    DETAILED = 'detailed',
    SUMMARIZED = 'summarized',
}

export enum OutputFormats {
    JSON = 'json',
    YAML = 'yaml',
    TABLE = 'table',
    TEXT = 'text',
    ROW_DELETE = 'row_delete',
}
